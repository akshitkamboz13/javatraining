package Java.Training.Basics;

public class Training03Loops {
    public static void main(String[] args) {
        /*

        // (initialization; condition; increment/decrement) AGHR INFINITE LOOP BAN GAYA TO INCREMENT YA DECREMENT MAI GALATY HOGI, AGHR CHALA HI NAHI TO CONDITION MAI GAATY HOGI.

        for(int i = 0; i < 5; i++)       //pehle code jayega initialization pr fir condition pr uske baad print hota h fir jata h increment pr.
        {
            System.out.println("Check");
        }

        int j=0;
                for (   ;j < 5;   )
                {
                    System.out.println("Check2");
                    j++;
                }

        //Table

        int a=5, A=1;
        for(int i=1; i<=10; i++)
        {
            A=a*i;
            System.out.println(a + " * " + i + " = " + A); // bina A=i*a k ham kr skhte h aghr print krne ali line mai ham likhenge : a + " * " + i + " = " + a*i
        }


        //Factorial
        int a=5, fac=1;
        for (int i =a; i>=1; i--)
        {
            fac = i*fac;
        }
        System.out.println("Factorial of " + a + " = " + fac);


        //Power

        int base = 2, power = 2, ans=1;
        for (int i=power; i>0; i--)
        {
            ans=base*ans;
        }
        System.out.println(ans);


        //Factor
        int a =6, c = 0;
        for (int i = 1; i<=a; i++)
        {
            if (a%i == 0)
            {
                c++;
                System.out.println(i);
            }
        }
        System.out.println("No. of factors are: " + c);


        //Prime number
        int a =25, c = 0;
        for (int i = 1; i<=a; i++)
        {
            if (a%i == 0)
            {
                c++;
                //System.out.println(i);
            }
        }
        //System.out.println("No. of factors are: " + c);

        if(c==2)
        {
            System.out.println("no is prime");
        }
        else
        {
            System.out.println("number is not prime");
        }

        */





    }
}
