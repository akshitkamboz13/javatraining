package Java.Training.Basics;

import javax.sound.midi.Soundbank;
import java.util.Scanner;

public class Training07 {
    public static void main(String[] args)

    {
        //First 10 prime numbers:
        /*
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the first number: ");
        int first = sc.nextInt();
        System.out.println("Enter the no of prime numbers you want: ");
        int n = sc.nextInt();
        System.out.println("The prime numbers among " + first + " and " + n + " are: ");
        int count2=0;
        for (int j=first; ; j++ )
        {
            int count = 0;
            for (int i = j; i >= 1; i--)
            {
                if (j % i == 0)
                {
                    count++;
                }
            }
            if (count == 2) {
                System.out.println(j);
                count2++;
            }
            if (count2==n)
            {
                break;
            }
        }

        */

        // Closest prime number to a given number
        /*
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the number: ");
        int num = sc.nextInt();
        int r=0, l=0;
        //System.out.println("Enter the no of prime numbers you want: ");
        //int n = sc.nextInt();

        int count2=0;
        for (int j=num;  ; j++ )
        {
            int count = 0;
            for (int i = j; i >= 1; i--)
            {
                if (j % i == 0)
                {
                    count++;
                }
            }
            if (count == 2) {
                //System.out.println(j);
                r=j;
                count2++;
            }
           if (count2==1)//edit
            {
                break;
            }
        }

         count2=0;
        for (int j=num;j>=1; j--)
        {
            int count = 0;
            for (int i = j; i >= 1; i--)
            {
                if (j % i == 0)
                {
                    count++;
                }
            }
            if (count == 2) {
                //System.out.println(j);
                l=j;
                count2++;
            }
            if (count2==1)//edit
            {
                break;
            }
        }
        if (num-l<r-num)
        {
            System.out.println("Nearest prime number from " + num + " is " +l);
        }
        else if (num-l>r-num)
        {
            System.out.println("Nearest prime number from " + num + " is " +r);
        }
        else if (l==r)
        {
            System.out.println("The number itself is prime: " + num);
        }

         */

        //Armstrong number among two given numbers:

        /*
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the first number: ");
        int start = sc.nextInt();
        System.out.println("Enter the second number: ");
        int end = sc.nextInt();

        for (int j=start; j<=end; j++)
        {
            int sum = 0;
            int count = 0;

            for (int i = j; i >=1; i = i / 10)
            {
                count++;
            }
            for (int i = j; i >=1; )
            {
                int lastDigit = i % 10;
                i = i / 10;
                sum = sum + (int) Math.pow(lastDigit, count);
            }
            if (sum == j)
            {
                System.out.println(sum);
            }
        }

         */

        //Pallindrome number among 2 given numbers:
        /*
         Scanner sc= new Scanner(System.in);
        System.out.println("Enter the first num: ");
        int num = sc.nextInt();
        System.out.println("Enter the second num: ");
        int num2 = sc.nextInt();
        for(int j=num; j<=num2; j++)
        {
            int rev = 0;

            for (int i = j; i > 0; )
            {
                int lastDigit = i % 10;
                rev = rev * 10 + lastDigit;
                i = i / 10;
            }

            if (j==rev)
            {
                System.out.println(rev);
            }

        }
        */
    }
}
