package Java.Training.OOPS.feb3;

public class deletion
{
    public static void main(String[] args)
    {
        String s = "ab3cd6hk2lpor";
                 // abhkor
        String t = "";

        for (int i = 0; i < s.length(); i++)
        {
            if (s.charAt(i)>= 48 && s.charAt(i) <= 57)
            {
                int x = s.charAt(i)-48;
                i = i + x;
                // continue; b use kr skhte h fir lekin else use nahi krna h pr else m jo code h woh if k bahir likhna hai.
            }
            else
            {
            t = t+s.charAt(i);
            }
        }
        System.out.println(t);
    }
}
